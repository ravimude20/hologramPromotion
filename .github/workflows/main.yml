name: Smart Code Reviewer

on:
  pull_request:
    types:
      - opened
      - synchronize
permissions: write-all
jobs:
  review:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repo
        uses: actions/checkout@v3

      - name: AI Code Reviewer
        uses: ravimude20/smartReviewer@main
        with:
          GITHUB_TOKEN: "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"
          exclude: "**/*.json, **/*.md" # Optional: exclude patterns separated by commas
